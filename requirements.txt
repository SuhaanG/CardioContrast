# Core dependencies for CardioContrast
# Install PyTorch separately from https://pytorch.org matching your CUDA version.
# Then install the rest:
#   pip install -r requirements.txt

torch
torchvision
nibabel
numpy
Pillow
requests
tqdm
regex
sacremoses
sentencepiece
filelock
packaging
tokenizers
